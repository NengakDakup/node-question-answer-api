const express = require('express');
const mongoose = require('mongoose');

// Load Question Model
const Profile = require('../models/Profile');

module.exports = function(){
  console.log('pointer doinker');
}
